version: '3.8'

services:
  app:
    build: .
    ports:
      - "8000:8000"
    environment:
      - PORT=8000
      - APP_ENV=production
      - APP_DEBUG=false
    volumes:
      - .:/var/www/html
    command: php -S 0.0.0.0:8000 -t public
